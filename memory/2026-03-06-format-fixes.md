# 2026-03-06 格式修复记录

## 问题来源

Ken 反馈三个格式问题：
1. **日期错误**：显示 UTC 时间而非北京时间
2. **缺少空行**：段落间缺少全角空行分隔
3. **时间不匹配**："明天X点见"与实际发送时间不一致

## 已完成的修复

### 1. Karpathy AI 博客精选（7:00）

**文件修改**：`/root/.openclaw/workspace/ai-daily-digest/ai-daily-digest/scripts/digest.ts`

**修改内容**：
```typescript
// 修复前
const now = new Date();
const dateStr = now.toISOString().split('T')[0];

// 修复后
const now = new Date();
const beijingOffset = 8 * 60 * 60 * 1000; // GMT+8
const beijingTime = new Date(now.getTime() + beijingOffset);
const dateStr = beijingTime.toISOString().split('T')[0];
```

**段落间距**：
```typescript
// 在每一点之间添加全角空行
report += `　\n\n`;
```

**标题加粗**：
```typescript
// 所有标题必须加粗
report += `### ${globalIndex}. **${a.titleZh || a.title}**\n\n`;
```

**动态时间**：
```typescript
// 根据实际执行时间计算"明天X点见"
const beijingHour = beijingTime.getUTCHours();
report += `\n*明天${beijingHour}点见* 🪭\n`;
```

---

### 2. AI 三巨头监控（7:05）

**Cron 任务 ID**：`d53a7a1c-98da-463e-a810-117e97de2bc6`

**修改内容**：在 message 中添加格式要求
```
**格式要求**：
- 段落间使用全角空行（　）分隔
- 关键信息加粗
- **结尾时间**：必须写"明天7:05见"（匹配执行时间 7:05）
```

---

### 3. 电子供应链每日情报（7:10）

**Cron 任务 ID**：`df51eeb3-0afb-499e-9dc8-2ec249da91d9`

**修改内容**：在 message 中添加格式要求
```
**格式要求（严格执行 v17）**：
1. 使用全角空格（　）分隔所有板块
2. 关键词加粗：标题、品类名、公司名、产品名、关键数据
3. 不要使用分隔线（━━━）或连续换行
4. **结尾时间**：必须写"明天7:10见"（匹配执行时间 7:10）
```

---

### 4. 供应链风险日报（7:15）

**Cron 任务 ID**：`0d516cee-5fe9-4cfa-942a-c0d39c022478`

**修改内容**：在 message 中添加格式要求
```
**格式要求**：
- 使用全角空格（　）分隔
- 标题加粗
- 红灯之间两个空行
- **结尾时间**：必须写"明天7:15见"（匹配执行时间 7:15）
```

---

### 5. 政策与法规监控日报（7:20）

**Cron 任务 ID**：`1b061019-3fc4-4dc0-aade-8d6de20ac6cb`

**修改内容**：在 message 中添加格式要求
```
**格式要求**：
- 快速预警异常风险
- 标题加粗、全角空格分隔
- **结尾时间**：必须写"明天7:20见"（匹配执行时间 7:20）
```

---

### 6. 宏观财务监控日报（7:25）

**Cron 任务 ID**：`17e273bd-c446-40e1-8a70-3ea981a55074`

**修改内容**：在 message 中添加格式要求
```
**格式要求**：
- TOP 3核心变化（一句话洞察）
- 原材料价格、汇率、利率
- 标注涨跌幅、预警阈值
- 标题加粗、全角空格分隔
- **结尾时间**：必须写"明天7:25见"（匹配执行时间 7:25）
```

---

## 核心原则（长期遵循）

1. **时间统一**：所有时间必须为北京时间（GMT+8）
2. **段落间距**：使用全角空行 `　`
3. **标题加粗**：所有标题和关键词必须加粗
4. **时间匹配**："明天X点见"必须匹配实际执行时间
5. **格式固化**：一旦格式固定，未经明确批准不得更改

---

## 验证方式

明天（2026-03-07）早上 7:00-7:30 的报告将验证：
- [ ] 日期是否为北京时间
- [ ] 段落间是否有全角空行
- [ ] 标题是否加粗
- [ ] "明天X点见"时间是否准确

---

## 附加修复：政策监控保密（7:45）

**Cron 任务 ID**：`1b061019-3fc4-4dc0-aade-8d6de20ac6cb`

**问题**：脚本输出了详细的内部框架信息（D1-D5、搜索数量等）

**文件修改**：`/root/.openclaw/workspace/skills/policy-regulation-monitor/run.sh`

**修改内容**：
```bash
# 修复前
echo "📂 D1 贸易政策（5个搜索）"
echo "   🔍 D1.1 美国对华关税政策..."
...
echo "   ✅ D1 完成（5个搜索）"
echo "   📂 D2 产业政策：6个搜索"
...
echo "   📊 总计：27个定向搜索"

# 修复后
# 静默模式，不输出框架信息
# 只在最后输出：
echo "✅ 数据收集完成（过去7天）"
echo "⏰ 完成时间：$(date)"
```

**Cron 任务更新**：
```
**⚠️ 重要：报告保密**
- 只说时间范围（如"过去7天"）
- 不要提及监控维度、框架、搜索数量等内部信息
- 不要说"D1/D2/D3"、"五维监控"、"27个搜索"等
```

**保密原则**：
- 内部监控框架（D1-D5）不对外暴露
- 搜索数量（27个）不对外暴露
- 只说时间范围（过去24小时/7天）
- 报告只包含发现，不包含方法论

---

## 所有修改验证清单

- [x] Karpathy 脚本修改（digest.ts）
- [x] 政策监控脚本修改（run.sh）
- [x] 6 个 cron 任务格式要求更新
- [x] 记录到 `/root/.openclaw/workspace/memory/2026-03-06-format-fixes.md`

---

_记录时间：2026-03-06 07:45 GMT+8_
_记录者：Claw1号_
_验证方式：明天早上的报告将验证所有修复_
